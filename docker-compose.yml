version: '3.8'

services:
  huddle-space:
    build: .
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
    # For a purely private experience, you can also spin up 
    # your own PeerJS server and point to it here if needed.
